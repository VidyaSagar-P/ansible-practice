- name: vars from file
  hosts: web
  vars_files:
  - vars.yaml
  tasks:
  - name: print the course info
    ansible.builtin.debgug:
      msg: "Hi, I am learning {{COURSE}}, Duration:{{DURATION}}, Trainer: {{TRAINER}}"