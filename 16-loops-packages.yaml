- name: install the packages
  hosts: web
  become: yes
  tasks:
  - name: install the given packages
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
    - nginx
    - mysql
    - httpd