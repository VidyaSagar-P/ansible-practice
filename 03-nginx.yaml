- name: Install and Run NGINX
  hosts: web
  become: yes
  tasks:
  - name: install NGINX
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run NGINX
    ansible.builtin.service:
      name: nginx
      state: stopped